# Comptabilité Automation - Python Dependencies
# Installation: pip3 install -r requirements.txt

# Web automation (Tier 1: fetch scripts)
playwright>=1.40.0

# Excel manipulation (Tier 3: import)
openpyxl>=3.0.0

# PDF processing (Tier 2: BG_GESTION format)
pdfplumber>=0.9.0

# OCR for virtual keyboard (Tier 1: SG fetch)
pytesseract>=0.3.0

# Image processing (screenshots, OCR)
Pillow>=9.0.0

# HTTP requests (quotes fetching)
requests>=2.28.0

# Notes:
# - playwright install chrome (Chrome browser for Playwright)
# - python3-tk must be installed separately (system package: sudo apt install python3-tk)
# - tesseract-ocr must be installed separately (system package)
# - xclip must be installed separately (system package: sudo apt install xclip) — clipboard pour Kraken 2FA
# - gpg must be available in PATH for credential decryption
# - Standard library modules (configparser, pathlib, subprocess, etc.) are included with Python
